name: CI/CD Pipeline

on:
  push:
    branches:
      - production

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Login to Docker Hub
      run: |
        echo "dckr_pat_HV4fQsWM3tZknlpEDB-k2kVIQe4" | docker login -u "zafarassidiq" --password-stdin

    - name: Build and Push Docker Image
      run: |
        docker build -t zafarassidiq/fe-dumbmerch .
        docker push zafarassidiq/fe-dumbmerch

    - name: SSH to App Server
      uses: appleboy/ssh-action@master
      with:
        host: 103.175.216.224
        username: appser
        key: |
          ${{ secrets.APP_SERVER_SSH_KEY }}
        script: |
          # Di sini Anda bisa menambahkan perintah-perintah untuk menjalankan Docker container di server, misalnya:
          docker-compose up -d
